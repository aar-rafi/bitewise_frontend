import { ChatInterface } from "@/components/chat/ChatInterface";

const Chat = () => {
  return (
    <div className="h-screen w-full">
      <ChatInterface />
    </div>
  );
};

export default Chat;
